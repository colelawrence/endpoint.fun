extends layout

block head-title
  | new function

block page-title
  | new function

block content
  form(action="/new-function", method="post")
    label(for="token") Token:
    input(name="token")
    label(for="fnbody") Function body:
    textarea.large-textarea(name="fnbody" placeholder="export function ...(...) {\n    ...\n}")
    p
      small
        i ⚠️ All exported functions must have at least one parameter!
    button(type="submit") Create
